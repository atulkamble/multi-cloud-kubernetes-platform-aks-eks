apiVersion: apps/v1
kind: Deployment
metadata:
  name: sample-microservice
  namespace: platform
spec:
  template:
    spec:
      containers:
        - name: app
          env:
            - name: CLUSTER
              value: aks
